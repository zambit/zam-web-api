// Package internal contains parts of application which used internally and would't be exported.
//
// Main intend to store there components, which handle some kind of external requests (web-requests, broker
// messages, etc), or APIs used internally
package internal
